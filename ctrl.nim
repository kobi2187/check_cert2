import docopt

import domain
import manage_reports, manage_certificate, manage_mailing

proc start*(args: Table[string, docopt.Value], cfg: UserInfo) =
  # starts real program
  discard


  # validate file exists, validate file structure, validate info.
  

  # start operation.
proc performAction() = discard
